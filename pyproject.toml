[project]
name = "todo-app"
version = "0.1.0"
description = "An in-memory todo console application"
requires-python = ">=3.13"
dependencies = []

[tool.setuptools.packages.find]
where = ["backend/src"]